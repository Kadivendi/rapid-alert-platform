server:
  port: 0

spring:
  application:
    name: url-shortener
  datasource:
    url: jdbc:tc:postgresql:15-alpine:///shortener
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

eureka:
  client:
    enabled: false
